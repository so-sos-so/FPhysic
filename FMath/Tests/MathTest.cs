using System;
using FMath;
using NUnit.Framework;

namespace Tests
{
    public class MathTest
    {
        [Test]
        public void Sin()
        {
            Console.WriteLine(FPMath.Sin(1.2));
            Console.WriteLine(FPMath.Sin(1.2));
            Console.WriteLine(FPMath.Sin(1.2));
            Console.WriteLine(FPMath.Sin(1.2));
        }
    }
}